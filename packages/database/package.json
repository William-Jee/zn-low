{
  "name": "@splash/database",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "studio": "prisma studio"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "latest",
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.3.3"
  }
}
